<aura:component implements="forceCommunity:availableForAllPageTypes,flexipage:availableForRecordHome,force:hasRecordId" access="global">
    
    <aura:attribute name="productType" Type="String"/>
    <aura:attribute name="submissionType" Type="String"/>
    <aura:attribute name="popUpProductType" Type="String"/>
    <aura:attribute name="campaignId" Type="String"/>

    <aura:registerEvent name="ConfigurationEvent" type="c:ConfigurationEvent"/>
    <!--<aura:handler name="init" value="{!this}" action="{!c.doInit}"/>-->
    <aura:handler name="render" value="{!this}" action="{!c.doInit}"/>
    <!--aura:handler name="change" value="{!v.productType}" action="{!c.changeConfig}"/-->
    
    
</aura:component>